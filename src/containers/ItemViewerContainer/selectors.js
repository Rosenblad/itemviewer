// import { createSelector } from 'reselect';
// import { getItems } from 'state/entities/selectors';

export const getHidden = state => state.itemViewer.hidden;

// export const getVisibleItems = (state, hidden) => 